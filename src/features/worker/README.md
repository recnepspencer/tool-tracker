# Worker feature boundary

Worker-facing features live in named sibling folders such as `worker-tools`, `worker-catalog`, and `worker-activity`. This parent boundary is reserved for cross-worker composition and does not own transport, fixtures, or domain state.
