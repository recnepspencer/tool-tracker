# Admin feature boundary

Admin-facing features live in named sibling folders such as `admin-dashboard`. This parent boundary is reserved for cross-admin composition and does not own transport, fixtures, or domain state.
